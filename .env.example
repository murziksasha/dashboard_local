# Server port (default 3000)
PORT=3000

# Optional: override data directory / DB path (useful for tests)
# DASHBOARD_DATA_DIR=./data
# DASHBOARD_DB_PATH=./data/dashboard.db

# Public URL behind a reverse proxy (OIDC redirects, email links)
# APP_BASE_URL=https://dashboard.lan.example
